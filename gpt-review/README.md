# GPT Review Workspace

This directory stores GPT second-review artifacts for the GPU Server SIT test
library.

Planned contents:

- `REVIEW_WORKFLOW_LOGIC_GPT_SECOND_REVIEW.md` — GPT second-review policy.
- `test_gpt_review.json` — GPT review overlay keyed by testcase `code`.
- Batch audit reports for human review.
- Session handoff and progress checkpoints.

The original testcase sources remain unchanged:

- `data/REVISED_commands_merged_with_raw.xlsx`
- `data/tests.json`
